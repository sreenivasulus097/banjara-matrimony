import Layout from "@/components/Layout";
import SignUp from "@/components/SignUp";
import React from "react";

type Props = {};

const signup = (props: Props) => {
  return (
    <Layout pageTitle="Banjara Matrimony - Sign Up">
      <SignUp />
    </Layout>
  );
};

export default signup;
